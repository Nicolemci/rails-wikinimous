<%= render "navbar" %>

<div id="articles">
  <% @articles.each do |article| %>
    <strong><%= link_to article.title, article %></strong>
    <% if article.content.present? %>
      <%= "#{article.content[0..170]}...." %>
    <% else %>
      No content available.
    <% end %>
  <% end %>
</div>
